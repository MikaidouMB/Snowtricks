{% extends 'base.html.twig' %}

{% block title %}Modifier Utilisateur{% endblock %}

{% block body %}
<div class="wrapper-form">
    <div class="container">
        <h1>Modifier utilisateur</h1>
            {{ form_start(userForm) }}
        <div class="col-sm-3 mb-4 ">
            {{ form_row(userForm.email) }}
        </div>
        <div class="col-md-6 mb-4 ">
            {{ form_row(userForm.roles) }}
            <button type="submit" class="btn btn-primary d-block mt-5" title="">
                valider
            </button>
        </div>
    </div>
</div>
    {{ form(userForm) }}
    <a class="nav-link text-center" href="{{ path('admin_users') }}">retour à la liste d'utilisateurs</a>
{% endblock %}
