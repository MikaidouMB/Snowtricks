{% extends 'base.html.twig' %}

{% block title %}Editer figure{% endblock %}

{% block body %}

    <div class="container">
        <h1>Editer figure</h1>

        {{ include('trick/_form_message.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('trick_index') }}">back to list</a>

        {{ include('trick/_delete_form.html.twig') }}
    </div>

{% endblock %}
